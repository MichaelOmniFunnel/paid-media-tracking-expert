you previously did this research

Great — I've done a thorough investigation across PageSpeed Insights, the Lighthouse Treemap, and the actual checkout page code. Here's a full breakdown of what needs to happen and why, organized by priority:

What I Found

This is a NetSuite SuiteCommerce Advanced site. That's important because it means some things (like the core JS bundling) are partly controlled by NetSuite's platform, and others are fully in your control via extensions and customizations.

Problem 1: Render-Blocking JavaScript in the Page Body (Most Impactful)

Estimated savings: 1,200msThe biggest single issue is that several large JavaScript files are loaded synchronously — meaning the browser has to fully download and execute each one before it can even start showing the page to the user. These files are loaded with no async or defer attribute:

/scs/javascript/checkout.js — 438 KiB, takes 1,130ms

/scs/extensions/checkout-templates_5.js — 267 KiB, takes 950ms

/scs/extensions/checkout_5.js — 172 KiB, takes 640ms

/cms/3/cms.js — 52 KiB, takes 370ms

google.com/recaptcha/api.js — loaded twice, synchronously

/scs/checkout.environment.shortcache.ssp and /scs/customFieldsMetadata.sspWhat needs to happen: These scripts — especially the checkout and extension bundles — need to be loaded with defer so they don't block the page from rendering. The recaptcha/api.js is also being loaded twice, which is pure waste and needs to be deduplicated. On SuiteCommerce, the core files (checkout.js, checkout-templates_5.js, checkout_5.js) are generated by NetSuite's build system, so fixing the blocking load would require either modifying the SCA theme's HTML templates to add defer, or working with NetSuite's configuration.

Problem 2: reCAPTCHA Loaded 4 Times (Major Waste)

The reCAPTCHA script is being loaded 4 times total — twice from google.com/recaptcha/api.js (blocking) and twice more as async from gstatic.com. This is almost certainly a bug in the BluePoint.ReCaptcha extension you have installed, or duplicate calls between the extension and the base theme. Each copy is 358 KiB. This is contributing ~789 KiB of the 1,709 KiB of "unused JavaScript." What needs to happen: Audit the ReCaptcha extension configuration and the theme templates to ensure reCAPTCHA is only initialized once.

Problem 3: Cache Lifetimes Too Short (1,414 KiB wasted on repeat visits)

All your first-party files — checkout.js, checkout-templates_5.js, the CSS, fonts — are only cached for 2 hours. That means every visitor who comes back within the same day has to re-download everything. Google recommends at least 1 year for static assets (using cache-busting via versioned filenames, which your site already does with the ?t=1770834117652 timestamp parameter).What needs to happen: The HTTP cache headers for your JS, CSS, and font files need to be changed to set a much longer Cache-Control: max-age — ideally 1 year (31,536,000 seconds). Since the files already have a version timestamp in the URL, there's no risk of users getting stale files. This is a server/CDN configuration change. On NetSuite SuiteCommerce, you may need to raise a support case or look at CDN settings, as these headers are often controlled by the platform.

Problem 4: The LCP Element is a Text Paragraph (Not an Image)

LCP is 4.6s in real users, with 5,790ms of "element render delay"PageSpeed identified your Largest Contentful Paint element as this text paragraph in the New Customer section:

"Create an account and take advantage of faster checkouts..."The reason it takes 5.79 seconds to appear is entirely because of the blocking JavaScript above — the scripts are holding up rendering of the whole page. The Time to First Byte is fine (0ms delay there), so the server is fast. The problem is purely the JS blocking the browser from painting the page. Fix Problem 1 and this problem largely resolves itself.

Problem 5: Layout Shift (CLS: 0.22 in real users — FAILING)

Multiple things are causing elements to jump around as the page loads:a) The header logo — The <img> for the Verocious logo has width="700" height="124" set in HTML, which is good, but the logo is still listed as an "unsized image element" causing layout shift. The logo is being loaded via CSS or the image dimensions aren't being respected during the initial paint. It appears the logo shifts position multiple times during load.b) The main container — <div id="main-container"> has a min-height of 315px set inline via JavaScript, but this is being set after the page renders, causing the content area to suddenly grow. The value should be set statically in CSS, not applied by a script.c) Form controls — The Password field and Email Address field are also shifting, likely because the reCAPTCHA widget or form validation styling is being injected dynamically after the initial render.What needs to happen: The main-container min-height should be hardcoded in CSS (not set via JS). The logo's CSS should explicitly reserve its space before the image loads. Any reCAPTCHA widget containers should have their dimensions pre-allocated in CSS so they don't cause the page to jump when the widget loads.

Problem 6: 1,709 KiB of Unused JavaScript

The full breakdown of wasted JavaScript:

reCAPTCHA (Google CDN): ~790 KiB wasted — loaded 4 times, mostly unused

Your first-party files: ~633 KiB wasted — checkout.js (258 KiB unused), checkout-templates_5.js (242 KiB unused), checkout_5.js (110 KiB unused)

Google Tag Manager: ~217 KiB wasted — multiple GTM/gtag containers running

Facebook Pixel: ~69 KiB wastedThe first-party waste is largely inherent to SuiteCommerce's bundled approach — the checkout bundle includes code for all checkout steps even when you're on the login page. The most actionable fix here is the reCAPTCHA duplication, and potentially evaluating whether all the Google Tag Manager containers (there appear to be 4 different GTM/gtag scripts) are all necessary.

Problem 7: 181 KiB of Unused CSS

The single CSS file checkout_5.css (159 KiB) has a lot of unused rules. This is largely a SuiteCommerce platform issue — the theme bundles all CSS together. What needs to happen: This likely requires splitting the CSS so only the styles needed for the login/register step are loaded first, with the rest deferred. This is a customization to the SCA theme.

Problem 8: No Font Preloading

Your site uses custom web fonts (DroidSans, OpenSans, FontAwesome) that are loaded only when the CSS file is parsed. None of these fonts have <link rel="preload"> hints in the HTML, so the browser discovers them late.What needs to happen: Add <link rel="preload" as="font" crossorigin> tags in the <head> for your most critical font files (the DroidSans and OpenSans variants). This tells the browser to start downloading fonts immediately rather than waiting to parse the CSS.

Summary: What To Fix and Who Does It

Things you can likely fix in NetSuite/SuiteCommerce:

Fix the reCAPTCHA extension to stop loading it 4 times — check the BluePoint.ReCaptcha extension settings and your theme templates

Add defer to blocking scripts in the SCA theme HTML templates

Set main-container min-height in CSS instead of via JavaScript

Add font preload links to the checkout page <head> template

Add explicit dimensions/space reservation for reCAPTCHA widgets in CSSThings that likely require NetSuite support or server/CDN config:

Extend cache TTLs on static assets to 1 year

Reduce the CSS bundle size (longer-term SCA customization)Things worth reviewing with your marketing team:

Audit Google Tag Manager — you appear to have 4 separate GA/GTM scripts running. Some may be duplicates

Evaluate Facebook Pixel — is it needed on the checkout login page specifically? The single highest-value fix is addressing the render-blocking scripts and the reCAPTCHA duplication — fixing those two things alone would likely bring your LCP from 4.6s down significantly and could flip your Core Web Vitals from Fail to Pass.

 

how much of an impact are gtm and meta pixel in this scope

 

Here's the honest picture after digging into the actual timing and data:

The Short Answer

GTM and Meta Pixel are not causing your Core Web Vitals failure. They are a real weight problem, but they load after the damage is already done. That said, there's some significant cleanup worth doing — and there's actually a more interesting finding buried in there.

Timing Is Everything

This is the key discovery. Your blocking scripts hold the browser hostage for roughly the first 3+ seconds of page load. GTM and Facebook Pixel don't even start loading until ~3,359ms into the page load — well after the render-blocking scripts have already caused all the LCP delay. So they are not the cause of your failing Core Web Vitals. They're passengers on a slow bus, not the reason the bus is slow.

What GTM and Meta Pixel Actually Weigh

From the Lighthouse data:Google Tag Manager ecosystem: 592 KiB total, 217 KiB wasted (37% unused)This breaks down across 4 separate script requests — gtm.js, two gtag/js files, and the Google Ads conversion script. It represents about 17% of the total 3.4 MB JavaScript on the page.Meta Pixel: 211 KiB total, 69 KiB wasted (33% unused)Two requests — fbevents.js and the signals config. About 6% of total JS.Combined: ~804 KiB, ~23% of the total JS payload. That's not nothing, but since both are loaded async and well after page render, they don't affect LCP, FCP, or TBT in any meaningful way.

The More Interesting Finding: You Have Both Client-Side AND Server-Side GTM Running

This is worth flagging. You have sst.verociousmotorsports.com — that's a server-side Google Tag Manager setup. During the page load, I can see GA4 data being sent to both:

analytics.google.com/g/collect (client-side)

sst.verociousmotorsports.com/g/collect (server-side) The whole point of server-side GTM is to move tracking off the client — reducing the JavaScript weight on the browser. But right now you're running both simultaneously, which means you're paying the performance cost of client-side tracking and the infrastructure cost of server-side tracking without getting the performance benefit. If server-side GTM is fully set up, the client-side GA4 and potentially the Facebook Pixel could be removed from the browser entirely and routed through the server container instead. That's where the real performance gain from GTM/Meta would come from.

The 7 Tag IDs Active on This Page

One more thing worth knowing: there are 7 different Google tag IDs firing on this page:

GTM-57T4T5BW — your main GTM container

G-MDZKSYLJ1B — GA4 property

G-862KMNBJX6 — a second GA4 property

AW-11060543839 — Google Ads conversion tracking

GT-KTPS29K, GT-5MGB3CV, GT-NM8HT5P — these are "Google tag" linked configurations, likely connecting Ads and GA4 Having two separate GA4 properties active on the same page is worth auditing — it may be intentional (e.g. one for a brand property, one for a rollup) but it's also a common leftover from migrations.

Bottom Line

GTM and Meta Pixel contribute roughly 800 KiB of JavaScript and about 286 KiB of that is wasted, but since they're all async and load after the page has already rendered (or failed to render in time), they are not fixable paths to improving your Core Web Vitals score. The path to fixing your score runs through the render-blocking scripts and reCAPTCHA duplication from Problem 1 and 2 in the previous analysis. Where GTM/Meta do matter is overall page weight and the server-side GTM consolidation opportunity — if you migrate Meta Pixel and GA4 to fire exclusively through the server-side container, you'd strip roughly 800 KiB of third-party JS off the page, which would meaningfully improve your Total Blocking Time and general page feel, just not the LCP metric specifically.

 

Here are the fixes ranked by impact on your Core Web Vitals (LCP and CLS are the two that are actually failing):

LCP — currently 4.6s, needs to be under 2.5s

Add defer to the blocking body scripts — checkout.js, checkout-templates_5.js, checkout_5.js, cms.js. These are holding the browser hostage for 3+ seconds before anything renders. This is the single biggest fix.

Fix reCAPTCHA loading 4 times — The BluePoint ReCaptcha extension is causing it to load twice as blocking + twice async. Deduplicate it to one async load. Saves ~790 KiB and eliminates unnecessary script execution.

Extend cache TTLs from 2 hours to 1 year on your JS/CSS/font files. Returning visitors are re-downloading everything on every visit. Your files already have version timestamps in the URL so there's no stale cache risk.

CLS — currently 0.22, needs to be under 0.14. Set main-container min-height in CSS, not via JavaScript. Right now a script sets it after render, causing the content area to jump. 5. Add explicit dimensions to the reCAPTCHA widget container in CSS so the browser reserves space for it before it loads. 6. Add width and height to the header logo in CSS to ensure space is reserved before the image loads (the logo is shifting multiple times during load).